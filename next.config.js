/** @type {import('next').NextConfig} */
const nextConfig = {
  // Permite servir el investor teaser como página estática en /investor
  // mientras el resto de la app corre en Next.js
};

module.exports = nextConfig;
